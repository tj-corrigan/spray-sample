machine:
  services:
    - docker

test:
  override:
    - sbt clean coverage test
  post:
    - pwd
    - cp /home/ubuntu/spray-sample/target/scala-2.11/scoverage-report/* ENV['CIRCLE_ARTIFACTS']

deployment:
  hub:
    branch: master
    commands:
      - sbt docker
