machine:
  services:
    - docker

test:
  override:
    - sbt coverage test
  post:
    - sbt coveralls
    - cp -r target/scala-2.11/scoverage-report/* $CIRCLE_ARTIFACTS

deployment:
  hub:
    branch: master
    commands:
      - sbt docker
