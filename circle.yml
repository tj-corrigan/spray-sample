machine:
  services:
    - docker

test:
  override:
    - sbt clean coverage test
  post:
    - cp target/scala-2.11/scoverage-report/* $CIRCLE_ARTIFACTS

deployment:
  hub:
    branch: master
    commands:
      - sbt docker
